---
phase: 04-mineru-pdf-fallback
plan: 01
type: execute
wave: 1
depends_on:
  - "03-01"
files_modified:
  - optional-skills/research/source-intake/scripts/intake_url.py
  - optional-skills/research/source-intake/scripts/run_mineru_backend.py
  - optional-skills/research/source-intake/references/backend-matrix.md
  - optional-skills/research/source-intake/references/packet-contract.md
  - optional-skills/research/source-intake/references/examples.md
  - tests/integration/test_source_intake_mineru.py
autonomous: false
requirements:
  - FALLBACK-02
user_setup:
  - service: mineru
    why: "PDF and complex-document verification needs a local CLI or service endpoint"
    env_vars:
      - name: MINERU_ENDPOINT
        source: "Local MinerU service URL or CLI wrapper configuration"
must_haves:
  truths:
    - "PDF URLs can route directly to MinerU"
    - "Poor Firecrawl PDF results can escalate to MinerU with explicit reasons"
    - "Packets keep traceable refs for raw PDF and extracted MinerU outputs"
  artifacts:
    - path: "optional-skills/research/source-intake/scripts/run_mineru_backend.py"
      provides: "MinerU backend wrapper"
      contains: "MINERU_ENDPOINT"
    - path: "tests/integration/test_source_intake_mineru.py"
      provides: "MinerU regression coverage"
      contains: "pdf_url"
  key_links:
    - from: "optional-skills/research/source-intake/scripts/intake_url.py"
      to: "optional-skills/research/source-intake/scripts/run_mineru_backend.py"
      via: "PDF routing and Firecrawl escalation"
      pattern: "run_mineru_backend"
---

<objective>
Add MinerU as the PDF and complex-document backend.

Purpose: Make the source-intake chain durable for PDFs, scanned documents, tables, and formula-heavy sources that native extraction or Firecrawl cannot normalize well.
Output: MinerU backend wrapper, PDF routing rules, artifact refs, and integration tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/prd/04-mineru-fallback.md
@optional-skills/research/source-intake/references/backend-matrix.md
@optional-skills/research/source-intake/references/packet-contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the MinerU backend wrapper and archive outputs</name>
  <files>optional-skills/research/source-intake/scripts/run_mineru_backend.py, optional-skills/research/source-intake/references/packet-contract.md</files>
  <read_first>.planning/prd/04-mineru-fallback.md, optional-skills/research/source-intake/references/packet-contract.md, .planning/codebase/CONCERNS.md</read_first>
  <action>Implement `run_mineru_backend.py` so it can call a configured MinerU CLI or service endpoint using `MINERU_ENDPOINT`, normalize the result into the shared intermediate payload, and emit refs for raw PDF, extracted JSON, and extracted Markdown paths. Update `packet-contract.md` so `refs.raw_pdf`, `refs.extracted_json`, and any MinerU-specific archive refs are explicitly documented.</action>
  <verify>rg -n "MINERU_ENDPOINT|raw_pdf|extracted_json|artifact_dir" optional-skills/research/source-intake/scripts/run_mineru_backend.py optional-skills/research/source-intake/references/packet-contract.md</verify>
  <acceptance_criteria>
    - `run_mineru_backend.py` contains `MINERU_ENDPOINT`
    - `packet-contract.md` contains `raw_pdf`
    - `packet-contract.md` contains `extracted_json`
  </acceptance_criteria>
  <done>MinerU can produce a packet-ready payload with explicit archive refs.</done>
</task>

<task type="auto">
  <name>Task 2: Add PDF routing and Firecrawl escalation rules</name>
  <files>optional-skills/research/source-intake/scripts/intake_url.py, optional-skills/research/source-intake/references/backend-matrix.md, optional-skills/research/source-intake/references/examples.md</files>
  <read_first>optional-skills/research/source-intake/scripts/intake_url.py, optional-skills/research/source-intake/references/backend-matrix.md, .planning/prd/04-mineru-fallback.md</read_first>
  <action>Extend `intake_url.py` so URLs classified as `pdf_url` route directly to MinerU, and Firecrawl PDF failures can escalate to MinerU when PDF quality is too low, the document is scan-heavy, multi-column, table-heavy, or formula-heavy. Update `backend-matrix.md` and `examples.md` to show both direct MinerU and Firecrawl-escalation paths using the same reason-code vocabulary as the code.</action>
  <verify>rg -n "pdf_url|run_mineru_backend|scan|multi-column|table|formula" optional-skills/research/source-intake/scripts/intake_url.py optional-skills/research/source-intake/references/backend-matrix.md optional-skills/research/source-intake/references/examples.md</verify>
  <acceptance_criteria>
    - `intake_url.py` contains `pdf_url`
    - `intake_url.py` contains a call to `run_mineru_backend`
    - `backend-matrix.md` documents direct PDF routing and Firecrawl escalation
  </acceptance_criteria>
  <done>PDF routing is explicit and consistent across code and docs.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Choose the MinerU verification mode for this environment</decision>
  <context>The phase can ship with mocked integration only, or it can also exercise a live local MinerU endpoint once the operator configures one.</context>
  <options>
    <option id="mock-only"><name>Mock-only verification</name><pros>No dependency on local services during default execution.</pros><cons>No live confidence signal for the service wrapper.</cons></option>
    <option id="live-smoke"><name>Live smoke verification</name><pros>Confirms the local MinerU path and archive refs really work.</pros><cons>Requires a configured local endpoint or CLI before execution can finish.</cons></option>
  </options>
  <resume-signal>Select: mock-only or live-smoke</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Add mocked PDF-path tests and optional live smoke coverage</name>
  <files>tests/integration/test_source_intake_mineru.py</files>
  <read_first>tests/integration/test_source_intake_firecrawl.py, .planning/codebase/TESTING.md, optional-skills/research/source-intake/scripts/run_mineru_backend.py</read_first>
  <action>Add an integration test file that covers direct `pdf_url` routing, Firecrawl-to-MinerU escalation, archive refs for raw PDF and extracted outputs, and unsupported behavior when MinerU is unavailable. Keep the default suite mocked, and gate any live smoke check behind explicit operator choice.</action>
  <verify>rg -n "pdf_url|run_mineru_backend|raw_pdf|extracted_json|UNSUPPORTED" tests/integration/test_source_intake_mineru.py</verify>
  <acceptance_criteria>
    - `test_source_intake_mineru.py` verifies `pdf_url`
    - `test_source_intake_mineru.py` verifies at least one archive ref such as `raw_pdf`
    - live MinerU verification is optional, not required for the default test command
  </acceptance_criteria>
  <done>MinerU behavior is regression-tested without making the whole repo depend on a local PDF service.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "run_mineru_backend|pdf_url|raw_pdf|extracted_json" optional-skills/research/source-intake/scripts optional-skills/research/source-intake/references`
- [ ] `rg -n "pdf_url|raw_pdf|UNSUPPORTED" tests/integration/test_source_intake_mineru.py`
- [ ] `uv run pytest tests/integration/test_source_intake_mineru.py -q`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- PDF handling remains traceable and bounded
- The optional skill can support complex documents without forcing MinerU into the default path
</success_criteria>

<output>
After completion, create `.planning/phases/04-mineru-pdf-fallback/04-01-SUMMARY.md`
</output>
