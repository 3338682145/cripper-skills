---
phase: 03-firecrawl-fallback
plan: 01
type: execute
wave: 1
depends_on:
  - "02-01"
files_modified:
  - optional-skills/research/source-intake/scripts/intake_url.py
  - optional-skills/research/source-intake/scripts/run_firecrawl_backend.py
  - optional-skills/research/source-intake/references/backend-matrix.md
  - optional-skills/research/source-intake/references/examples.md
  - tests/integration/test_source_intake_firecrawl.py
autonomous: false
requirements:
  - FALLBACK-01
user_setup:
  - service: firecrawl
    why: "Live fallback verification needs an API key for remote extraction"
    env_vars:
      - name: FIRECRAWL_API_KEY
        source: "Firecrawl dashboard API keys"
must_haves:
  truths:
    - "Native low-signal or JS-heavy pages can retry through Firecrawl"
    - "Fallback decisions emit explicit reason codes instead of silently passing or failing"
    - "The fallback path remains secondary to native extraction"
  artifacts:
    - path: "optional-skills/research/source-intake/scripts/run_firecrawl_backend.py"
      provides: "Firecrawl backend wrapper"
      contains: "FIRECRAWL_API_KEY"
    - path: "tests/integration/test_source_intake_firecrawl.py"
      provides: "Firecrawl fallback regression coverage"
      contains: "REVIEW_REQUIRED"
  key_links:
    - from: "optional-skills/research/source-intake/scripts/intake_url.py"
      to: "optional-skills/research/source-intake/scripts/run_firecrawl_backend.py"
      via: "fallback call for low-signal extraction"
      pattern: "run_firecrawl_backend"
---

<objective>
Add Firecrawl as the first external fallback backend for pages that native extraction cannot handle well.

Purpose: Preserve native-first behavior while giving the skill a deterministic escape hatch for JS-heavy or structurally weak pages.
Output: Firecrawl backend wrapper, selection heuristics, tests, and operator documentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/prd/03-firecrawl-fallback.md
@optional-skills/research/source-intake/references/backend-matrix.md
@src/cliper/adapters/web_url.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the Firecrawl backend wrapper and configuration handling</name>
  <files>optional-skills/research/source-intake/scripts/run_firecrawl_backend.py</files>
  <read_first>.planning/prd/03-firecrawl-fallback.md, optional-skills/research/source-intake/SKILL.md, .planning/codebase/INTEGRATIONS.md</read_first>
  <action>Implement `run_firecrawl_backend.py` with a clear client wrapper that reads `FIRECRAWL_API_KEY` from the environment, normalizes Firecrawl responses into the same intermediate payload shape as the native backend, and returns explicit reason codes for missing credentials, empty content, or backend failure. Do not bury credential errors under generic exceptions.</action>
  <verify>rg -n "FIRECRAWL_API_KEY|reason_codes|missing credential|empty content" optional-skills/research/source-intake/scripts/run_firecrawl_backend.py</verify>
  <acceptance_criteria>
    - `run_firecrawl_backend.py` contains `FIRECRAWL_API_KEY`
    - `run_firecrawl_backend.py` contains `reason_codes`
    - missing credential handling is explicit in code or error messages
  </acceptance_criteria>
  <done>Firecrawl is callable as a bounded fallback backend with explicit error reporting.</done>
</task>

<task type="auto">
  <name>Task 2: Add native-to-Firecrawl fallback heuristics to intake orchestration</name>
  <files>optional-skills/research/source-intake/scripts/intake_url.py, optional-skills/research/source-intake/references/backend-matrix.md, optional-skills/research/source-intake/references/examples.md</files>
  <read_first>optional-skills/research/source-intake/scripts/intake_url.py, optional-skills/research/source-intake/references/backend-matrix.md, src/cliper/adapters/web_url.py</read_first>
  <action>Extend `intake_url.py` so it evaluates native extraction quality and triggers Firecrawl when markdown is too short, heading structure is incomplete, noise ratio is high, or the page is identified as JS-heavy. Update `backend-matrix.md` and `examples.md` to document these reasons using the same reason-code vocabulary the code emits.</action>
  <verify>rg -n "too short|noise|JS-heavy|reason_codes|run_firecrawl_backend" optional-skills/research/source-intake/scripts/intake_url.py optional-skills/research/source-intake/references/backend-matrix.md optional-skills/research/source-intake/references/examples.md</verify>
  <acceptance_criteria>
    - `intake_url.py` contains a call to `run_firecrawl_backend`
    - `backend-matrix.md` documents low-signal and JS-heavy fallback rules
    - `examples.md` contains a Firecrawl fallback example with reason codes
  </acceptance_criteria>
  <done>Fallback routing is deterministic and documented with the same terms used in code.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Choose the Firecrawl verification mode for this environment</decision>
  <context>The phase can ship with mocked integration coverage only, or it can also run a live smoke test once `FIRECRAWL_API_KEY` is configured.</context>
  <options>
    <option id="mock-only"><name>Mock-only verification</name><pros>No external dependency blocking the phase, stable in CI and Windows shells.</pros><cons>No live Firecrawl smoke signal yet.</cons></option>
    <option id="live-smoke"><name>Live smoke verification</name><pros>Confirms the credential path and backend wiring really work.</pros><cons>Requires a valid API key and network access during execution.</cons></option>
  </options>
  <resume-signal>Select: mock-only or live-smoke</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Add mocked fallback tests and optional live smoke coverage</name>
  <files>tests/integration/test_source_intake_firecrawl.py</files>
  <read_first>tests/integration/test_ingest.py, .planning/codebase/TESTING.md, optional-skills/research/source-intake/scripts/intake_url.py</read_first>
  <action>Add a Firecrawl integration test file that mocks native low-signal output and Firecrawl success/failure responses. Verify fallback trigger reasons, backend_used changes, review-required behavior on failure, and the difference between mock-only and live-smoke paths without hard-requiring live credentials for the default test run.</action>
  <verify>rg -n "backend_used|Firecrawl|REVIEW_REQUIRED|reason_codes" tests/integration/test_source_intake_firecrawl.py</verify>
  <acceptance_criteria>
    - `test_source_intake_firecrawl.py` verifies `backend_used`
    - `test_source_intake_firecrawl.py` verifies a `REVIEW_REQUIRED` or failure route
    - live-smoke behavior is optional, not mandatory for the default test command
  </acceptance_criteria>
  <done>Firecrawl fallback is testable without making the whole phase depend on live credentials.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "run_firecrawl_backend|reason_codes|JS-heavy" optional-skills/research/source-intake/scripts/intake_url.py optional-skills/research/source-intake/scripts/run_firecrawl_backend.py`
- [ ] `rg -n "Firecrawl|backend_used|REVIEW_REQUIRED" tests/integration/test_source_intake_firecrawl.py`
- [ ] `uv run pytest tests/integration/test_source_intake_firecrawl.py -q`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Firecrawl is clearly secondary to native extraction
- Operators can execute the phase with either mocked or live verification intentionally
</success_criteria>

<output>
After completion, create `.planning/phases/03-firecrawl-fallback/03-01-SUMMARY.md`
</output>
