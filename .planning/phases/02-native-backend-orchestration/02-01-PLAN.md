---
phase: 02-native-backend-orchestration
plan: 01
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - src/cliper/models.py
  - src/cliper/service.py
  - src/cliper/adapters/web_url.py
  - optional-skills/research/source-intake/scripts/intake_url.py
  - optional-skills/research/source-intake/scripts/classify_url.py
  - optional-skills/research/source-intake/scripts/run_native_backend.py
  - optional-skills/research/source-intake/scripts/normalize_packet.py
  - optional-skills/research/source-intake/scripts/route_packet.py
  - optional-skills/research/source-intake/references/packet-contract.md
  - docs/migrations/2026-04-13-source-packet-v0-to-v1.md
  - tests/integration/test_source_intake_native.py
autonomous: true
requirements:
  - NATIVE-01
  - NATIVE-02
  - PACKET-01
  - PACKET-02
  - GOV-01
user_setup: []
must_haves:
  truths:
    - "A single URL can move through classify, native backend extraction, packet normalization, and baseline routing"
    - "The native backend reuses existing cliper extraction, dedupe, and manifest behavior instead of duplicating it"
    - "The normalized packet carries provenance, backend, routing, archive, and dedupe metadata"
  artifacts:
    - path: "optional-skills/research/source-intake/scripts/run_native_backend.py"
      provides: "Native backend wrapper"
      contains: "cliper"
    - path: "optional-skills/research/source-intake/scripts/normalize_packet.py"
      provides: "Source-packet normalization"
      contains: "source-packet"
    - path: "docs/migrations/2026-04-13-source-packet-v0-to-v1.md"
      provides: "Migration note for additive contract changes"
      contains: "source-packet"
  key_links:
    - from: "optional-skills/research/source-intake/scripts/run_native_backend.py"
      to: "src/cliper/service.py"
      via: "native wrapper imports existing services"
      pattern: "IngestService|ValidationService"
    - from: "optional-skills/research/source-intake/scripts/intake_url.py"
      to: "optional-skills/research/source-intake/scripts/normalize_packet.py"
      via: "main orchestration calls normalization"
      pattern: "normalize_packet"
---

<objective>
Build the native orchestration path that converts current `cliper` behavior into normalized `source-packet` output.

Purpose: Reuse the brownfield codebase instead of rewriting extraction and manifest logic in the optional skill.
Output: Native backend wrapper, packet normalization, baseline route handoff, migration notes, and native integration tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/prd/02-native-orchestration.md
@src/cliper/service.py
@src/cliper/models.py
@src/cliper/adapters/web_url.py
@docs/migrations/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose reusable native-backend primitives from the current cliper stack</name>
  <files>src/cliper/models.py, src/cliper/service.py, src/cliper/adapters/web_url.py, optional-skills/research/source-intake/scripts/run_native_backend.py</files>
  <read_first>src/cliper/service.py, src/cliper/models.py, src/cliper/adapters/web_url.py, .planning/codebase/ARCHITECTURE.md</read_first>
  <action>Add the smallest reusable seam needed for the optional skill to call native extraction without duplicating registry loading or DOM extraction. Expose a stable payload containing canonical URL, resolved URL, title, language, published_at, markdown body, content hash, dedupe key, and raw HTML. Implement `run_native_backend.py` as a wrapper that imports these existing primitives rather than reimplementing requests, readability, or manifest logic in the skill layer.</action>
  <verify>rg -n "canonical_url|content_hash|raw_html|IngestService|fetch_document" src/cliper optional-skills/research/source-intake/scripts/run_native_backend.py</verify>
  <acceptance_criteria>
    - `run_native_backend.py` imports from `cliper`
    - the reusable payload exposes `canonical_url`, `content_hash`, `dedupe_key`, and `raw_html`
    - no second HTTP-fetch implementation is introduced under the skill package
  </acceptance_criteria>
  <done>Native backend reuse is explicit and avoids a parallel extraction stack.</done>
</task>

<task type="auto">
  <name>Task 2: Implement URL classification, packet normalization, and baseline routing hooks</name>
  <files>optional-skills/research/source-intake/scripts/intake_url.py, optional-skills/research/source-intake/scripts/classify_url.py, optional-skills/research/source-intake/scripts/normalize_packet.py, optional-skills/research/source-intake/scripts/route_packet.py, optional-skills/research/source-intake/references/packet-contract.md</files>
  <read_first>.planning/prd/02-native-orchestration.md, optional-skills/research/source-intake/references/packet-contract.md, PLAN.md</read_first>
  <action>Implement `classify_url.py` to emit only the v1 source types: `blog_article`, `docs_page`, `paper_page`, `pdf_url`, `github_repo`, `unknown`. Implement `normalize_packet.py` so the output Markdown frontmatter uses `schema: source-packet`, includes `backend_used`, `routing.suggested_topic`, `routing.suggested_kind`, and refs for archive artifacts. Implement `route_packet.py` as a baseline writer that can place packets into configured raw or review targets once a verdict is supplied, but do not add the final verifier logic yet.</action>
  <verify>rg -n "blog_article|docs_page|paper_page|pdf_url|github_repo|schema: source-packet|backend_used|suggested_topic" optional-skills/research/source-intake/scripts optional-skills/research/source-intake/references/packet-contract.md</verify>
  <acceptance_criteria>
    - `classify_url.py` contains all six v1 source types
    - `normalize_packet.py` contains `schema: source-packet`
    - `normalize_packet.py` contains `backend_used`
    - `route_packet.py` contains raw and review destination handling without hard-coding wiki paths into `src/cliper/`
  </acceptance_criteria>
  <done>Native results are converted into the milestone packet contract and can be routed by later phases.</done>
</task>

<task type="auto">
  <name>Task 3: Add migration notes and native-path integration tests</name>
  <files>docs/migrations/2026-04-13-source-packet-v0-to-v1.md, tests/integration/test_source_intake_native.py</files>
  <read_first>docs/migrations/README.md, tests/integration/test_ingest.py, .planning/codebase/TESTING.md</read_first>
  <action>Write a migration note explaining how `source-packet` extends the existing raw-asset workflow without replacing it. Add a native integration test file that exercises classify -> native backend -> normalize -> route using mocked network responses and verifies canonical URL dedupe, packet frontmatter, archive refs, and baseline route selection.</action>
  <verify>rg -n "source-packet|raw-asset|canonical_url|backend_used|artifact_dir" docs/migrations/2026-04-13-source-packet-v0-to-v1.md tests/integration/test_source_intake_native.py</verify>
  <acceptance_criteria>
    - the migration note contains both `source-packet` and `raw-asset`
    - `test_source_intake_native.py` verifies `canonical_url`
    - `test_source_intake_native.py` verifies `backend_used`
    - `test_source_intake_native.py` verifies an archive ref such as `artifact_dir` or `source_snapshot`
  </acceptance_criteria>
  <done>Contract governance and native-path regression coverage exist before fallback work begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "schema: source-packet|backend_used|routing" optional-skills/research/source-intake/scripts`
- [ ] `rg -n "source-packet|raw-asset" docs/migrations/2026-04-13-source-packet-v0-to-v1.md`
- [ ] `uv run pytest tests/integration/test_source_intake_native.py -q`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Native backend reuse is explicit in code
- A normalized packet exists without violating the `cliper` core boundary
</success_criteria>

<output>
After completion, create `.planning/phases/02-native-backend-orchestration/02-01-SUMMARY.md`
</output>
