---
phase: 06-github-placeholder-adapter
plan: 01
type: execute
wave: 1
depends_on:
  - "05-01"
files_modified:
  - optional-skills/research/source-intake/scripts/classify_url.py
  - optional-skills/research/source-intake/scripts/intake_url.py
  - optional-skills/research/source-intake/scripts/normalize_packet.py
  - optional-skills/research/source-intake/references/examples.md
  - tests/integration/test_source_intake_github.py
autonomous: true
requirements:
  - GITHUB-01
user_setup: []
must_haves:
  truths:
    - "GitHub URLs are classified separately from article, docs, and PDF inputs"
    - "GitHub inputs generate placeholder packets with UNSUPPORTED verdict"
    - "No GitHub flow attempts repository crawling, cloning, or wiki generation"
  artifacts:
    - path: "optional-skills/research/source-intake/scripts/classify_url.py"
      provides: "GitHub URL classification"
      contains: "github_repo"
    - path: "tests/integration/test_source_intake_github.py"
      provides: "GitHub placeholder regression coverage"
      contains: "UNSUPPORTED"
  key_links:
    - from: "optional-skills/research/source-intake/scripts/intake_url.py"
      to: "optional-skills/research/source-intake/scripts/normalize_packet.py"
      via: "placeholder packet generation"
      pattern: "UNSUPPORTED"
---

<objective>
Add the GitHub placeholder adapter that keeps the v1 boundary explicit.

Purpose: Recognize GitHub URLs cleanly without letting the intake system drift into repository crawling or code ingestion.
Output: GitHub URL classification, placeholder packet generation, and regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/prd/06-github-placeholder.md
@optional-skills/research/source-intake/references/examples.md
@PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend URL classification to recognize GitHub inputs explicitly</name>
  <files>optional-skills/research/source-intake/scripts/classify_url.py</files>
  <read_first>.planning/prd/06-github-placeholder.md, optional-skills/research/source-intake/scripts/classify_url.py, PLAN.md</read_first>
  <action>Update `classify_url.py` so any GitHub repository, tree, blob, issue, or pull-request URL resolves to `github_repo` for v1 purposes. Do not create subtypes that imply deeper ingestion. Keep the classifier bounded to the single placeholder route.</action>
  <verify>rg -n "github_repo|github.com" optional-skills/research/source-intake/scripts/classify_url.py</verify>
  <acceptance_criteria>
    - `classify_url.py` contains `github_repo`
    - `classify_url.py` contains `github.com`
    - no new deep-ingestion subtype such as `github_blob_markdown` or `github_repo_clone` is introduced
  </acceptance_criteria>
  <done>GitHub inputs are recognized without expanding scope.</done>
</task>

<task type="auto">
  <name>Task 2: Generate placeholder packets with fixed unsupported routing</name>
  <files>optional-skills/research/source-intake/scripts/intake_url.py, optional-skills/research/source-intake/scripts/normalize_packet.py, optional-skills/research/source-intake/references/examples.md</files>
  <read_first>optional-skills/research/source-intake/scripts/intake_url.py, optional-skills/research/source-intake/scripts/normalize_packet.py, optional-skills/research/source-intake/references/examples.md</read_first>
  <action>Update the main orchestration so `github_repo` bypasses the content backends, generates a placeholder `source-packet`, sets `backend_used: github_placeholder`, fixes the verdict to `UNSUPPORTED`, and adds reason codes that explain GitHub is intentionally deferred. Update `examples.md` to show the exact placeholder fields and route behavior.</action>
  <verify>rg -n "github_placeholder|UNSUPPORTED|reason_codes" optional-skills/research/source-intake/scripts/intake_url.py optional-skills/research/source-intake/scripts/normalize_packet.py optional-skills/research/source-intake/references/examples.md</verify>
  <acceptance_criteria>
    - `intake_url.py` contains `github_placeholder`
    - `normalize_packet.py` or orchestration code contains `UNSUPPORTED`
    - `examples.md` shows a GitHub placeholder packet example
  </acceptance_criteria>
  <done>GitHub behavior is explicit, stable, and safe for later review workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Add placeholder regression tests that guard the v1 boundary</name>
  <files>tests/integration/test_source_intake_github.py</files>
  <read_first>.planning/codebase/TESTING.md, optional-skills/research/source-intake/scripts/classify_url.py, optional-skills/research/source-intake/scripts/intake_url.py</read_first>
  <action>Add tests for repository, blob, and issue URLs. Verify classification to `github_repo`, placeholder packet creation, `backend_used: github_placeholder`, `verdict: UNSUPPORTED`, and the absence of code paths that invoke crawling, cloning, or wiki generation.</action>
  <verify>rg -n "github_repo|github_placeholder|UNSUPPORTED|clone|crawl" tests/integration/test_source_intake_github.py</verify>
  <acceptance_criteria>
    - `test_source_intake_github.py` verifies `github_repo`
    - `test_source_intake_github.py` verifies `github_placeholder`
    - `test_source_intake_github.py` verifies `UNSUPPORTED`
  </acceptance_criteria>
  <done>Regression coverage now protects the scope boundary from future GitHub creep.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "github_repo|github_placeholder|UNSUPPORTED" optional-skills/research/source-intake/scripts`
- [ ] `rg -n "github_repo|github_placeholder|UNSUPPORTED" tests/integration/test_source_intake_github.py`
- [ ] `uv run pytest tests/integration/test_source_intake_github.py -q`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- GitHub support stays explicitly unsupported beyond placeholder packets
- The milestone ends with scope boundaries encoded in code and tests
</success_criteria>

<output>
After completion, create `.planning/phases/06-github-placeholder-adapter/06-01-SUMMARY.md`
</output>
