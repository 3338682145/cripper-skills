---
phase: 05-verifier-and-cron
plan: 01
type: execute
wave: 1
depends_on:
  - "04-01"
files_modified:
  - optional-skills/research/source-intake/scripts/intake_url.py
  - optional-skills/research/source-intake/scripts/verify_packet.py
  - optional-skills/research/source-intake/scripts/route_packet.py
  - optional-skills/research/source-intake/scripts/cron_drain_queue.py
  - optional-skills/research/source-intake/references/verifier-rubric.md
  - optional-skills/research/source-intake/references/packet-contract.md
  - tests/integration/test_source_intake_verifier.py
  - tests/integration/test_source_intake_cron.py
autonomous: true
requirements:
  - VERIFY-01
  - ROUTE-01
  - ROUTE-02
  - CRON-01
user_setup: []
must_haves:
  truths:
    - "Verifier output uses only AUTO_PASS, REVIEW_REQUIRED, LOW_SIGNAL, or UNSUPPORTED"
    - "Only AUTO_PASS packets can enter the wiki raw inbox"
    - "Queue runs and manual runs share one pipeline and dedupe on canonical_url"
    - "Empty cron runs emit [SILENT]"
  artifacts:
    - path: "optional-skills/research/source-intake/scripts/verify_packet.py"
      provides: "Verifier verdict implementation"
      contains: "AUTO_PASS"
    - path: "optional-skills/research/source-intake/scripts/cron_drain_queue.py"
      provides: "Queue and cron orchestration"
      contains: "queue.jsonl"
    - path: "tests/integration/test_source_intake_cron.py"
      provides: "Cron behavior regression coverage"
      contains: "[SILENT]"
  key_links:
    - from: "optional-skills/research/source-intake/scripts/intake_url.py"
      to: "optional-skills/research/source-intake/scripts/verify_packet.py"
      via: "main orchestration requests a verifier verdict"
      pattern: "verify_packet"
    - from: "optional-skills/research/source-intake/scripts/cron_drain_queue.py"
      to: "optional-skills/research/source-intake/scripts/intake_url.py"
      via: "queue draining reuses the same pipeline"
      pattern: "intake_url"
---

<objective>
Implement the quality gate and cron path that make source intake durable over time.

Purpose: Enforce verdicts, routing, dedupe, and queue behavior before the skill can be trusted to run unattended.
Output: Verifier implementation, route enforcement, queue draining, and cron-specific tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/prd/05-verifier-and-cron.md
@optional-skills/research/source-intake/references/verifier-rubric.md
@optional-skills/research/source-intake/references/packet-contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the verifier contract and deterministic verdict logic</name>
  <files>optional-skills/research/source-intake/scripts/verify_packet.py, optional-skills/research/source-intake/references/verifier-rubric.md</files>
  <read_first>.planning/prd/05-verifier-and-cron.md, optional-skills/research/source-intake/references/verifier-rubric.md, PLAN.md</read_first>
  <action>Implement `verify_packet.py` so it returns only the four approved verdicts plus `confidence`, `signal_level`, `reason_codes`, `summary`, and any routing hints. Keep the verifier bounded to quality gating. It must not generate wiki prose, topic notes, or publishing output. Update `verifier-rubric.md` so the code and docs use the same verdict and reason-code vocabulary.</action>
  <verify>rg -n "AUTO_PASS|REVIEW_REQUIRED|LOW_SIGNAL|UNSUPPORTED|signal_level|reason_codes|summary" optional-skills/research/source-intake/scripts/verify_packet.py optional-skills/research/source-intake/references/verifier-rubric.md</verify>
  <acceptance_criteria>
    - `verify_packet.py` contains all four verdict strings
    - `verify_packet.py` contains `confidence`, `signal_level`, and `reason_codes`
    - verifier code or docs explicitly say it does not generate wiki or output content
  </acceptance_criteria>
  <done>Verifier behavior is deterministic and scoped to gating, not summarization.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce routing and dedupe across raw, review, and queue paths</name>
  <files>optional-skills/research/source-intake/scripts/intake_url.py, optional-skills/research/source-intake/scripts/route_packet.py, optional-skills/research/source-intake/references/packet-contract.md, tests/integration/test_source_intake_verifier.py</files>
  <read_first>optional-skills/research/source-intake/scripts/route_packet.py, optional-skills/research/source-intake/references/packet-contract.md, .planning/prd/05-verifier-and-cron.md</read_first>
  <action>Update `intake_url.py` and `route_packet.py` so packets are verified before routing, only `AUTO_PASS` lands in the configured raw inbox, and every other verdict lands in the review directory. Enforce canonical URL dedupe across both route destinations. Add integration tests that verify raw/review separation, canonical URL dedupe, and stable frontmatter fields after routing.</action>
  <verify>rg -n "AUTO_PASS|REVIEW_REQUIRED|LOW_SIGNAL|UNSUPPORTED|canonical_url|review" optional-skills/research/source-intake/scripts tests/integration/test_source_intake_verifier.py</verify>
  <acceptance_criteria>
    - `route_packet.py` contains explicit handling for all four verdicts
    - `route_packet.py` routes only `AUTO_PASS` to raw
    - `test_source_intake_verifier.py` verifies `canonical_url` dedupe across route paths
  </acceptance_criteria>
  <done>Routing is policy-driven and protected from duplicate writes.</done>
</task>

<task type="auto">
  <name>Task 3: Add queue draining and cron behavior with [SILENT] semantics</name>
  <files>optional-skills/research/source-intake/scripts/cron_drain_queue.py, tests/integration/test_source_intake_cron.py</files>
  <read_first>.planning/prd/05-verifier-and-cron.md, optional-skills/research/source-intake/scripts/intake_url.py, .planning/codebase/TESTING.md</read_first>
  <action>Implement `cron_drain_queue.py` so it reads `~/.hermes/intake/queue.jsonl`, skips already-processed or duplicate canonical URLs, calls the same `intake_url.py` pipeline for each item, and returns `[SILENT]` when the queue is empty or yields no new work. Add cron integration tests for empty queue, mixed verdict summaries, and dedupe-before-run behavior.</action>
  <verify>rg -n "queue.jsonl|\\[SILENT\\]|canonical_url|intake_url" optional-skills/research/source-intake/scripts/cron_drain_queue.py tests/integration/test_source_intake_cron.py</verify>
  <acceptance_criteria>
    - `cron_drain_queue.py` contains `queue.jsonl`
    - `cron_drain_queue.py` contains `[SILENT]`
    - `test_source_intake_cron.py` verifies empty queue output and mixed-verdict summary behavior
  </acceptance_criteria>
  <done>Cron behavior is explicit, deduped, and reuse-based instead of a second orchestration path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "AUTO_PASS|REVIEW_REQUIRED|LOW_SIGNAL|UNSUPPORTED" optional-skills/research/source-intake/scripts/verify_packet.py optional-skills/research/source-intake/scripts/route_packet.py`
- [ ] `rg -n "\\[SILENT\\]|queue.jsonl|canonical_url" optional-skills/research/source-intake/scripts/cron_drain_queue.py tests/integration/test_source_intake_cron.py`
- [ ] `uv run pytest tests/integration/test_source_intake_verifier.py tests/integration/test_source_intake_cron.py -q`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Routing policy is fully encoded in code and tests
- Manual and queued runs share one verified pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/05-verifier-and-cron/05-01-SUMMARY.md`
</output>
